# AllInOne 项目说明

> 最后更新：2026-07-02
> 版本：v0.6
> 技术栈：Tauri 2.0 + Vue 3 + TypeScript + Pinia + Vite

---

## 一、项目概述

AllInOne 是一个**多工具合一**的本地桌面应用，目前在同一个壳下集成四个独立工具：

| 工具 | 定位 | 存储媒介 |
| --- | --- | --- |
| 📋 **清单** | 个人待办 / 日报 | Markdown 文件（Obsidian 兼容） |
| ⚡ **GitFast** | 多仓库 git 快速操作 | YAML 配置文件 |
| 📜 **Script Runner** | 多仓库脚本批处理 | YAML 配置文件 |
| 🔀 **Code Review** | 公司内部代码评审创建 | YAML 配置文件 |

### 核心价值

- **多工具不杂糅**：每个工具独立 `features/<name>/` 目录，代码、状态、配置文件完全分离
- **文件即数据**：清单用 `.md`、GitFast/Script Runner/Review 用 `.yaml`，均可用任意编辑器修改
- **轻量级**：Tauri 安装包 5-10MB，运行内存 20-100MB（相比 Electron 节省 80%+）
- **零网络依赖**：纯本地运行，数据不离开设备
- **跨平台**：Windows / macOS / Linux 均可运行

### 四工具关系图

```
                ┌─ 📋 清单工具（独立 features/checklist/）
                │   └─ .md 文件存储（Obsidian 兼容）
                │
                ├─ ⚡ GitFast（独立 features/gitfast/）
                │   └─ 终端（单仓库 PTY 交互）
                │
AllInOne ───────┼─ 📜 Script Runner（独立 features/scripts/）
                │   └─ 脚本定时执行 + 日志
                │
                └─ 🔀 Code Review（独立 features/review/）
                    └─ 评审创建（参数化脚本执行 + 日志）
```

> 四工具代码零交叉，仅共享顶层的 `Toolbar.vue`（工具切换器）和 `stores/tool.ts`（当前激活工具状态）。

---

## 二、功能清单

### 2.1 清单工具

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| F1 | 选择文件夹作为工作区 | 系统级目录选择对话框 |
| F2 | 枚举工作区下所有 .md 文件 | 侧边栏列表，置顶文件在前 |
| F3 | 打开清单查看 | 解析 MD 渲染为可交互清单 |
| F4 | 勾选 / 取消勾选 | 即时回写文件 |
| F5 | 添加待办项 | **插入到"待办"段段首**（最新在最上面） |
| F6 | 编辑待办项文本 | 双击或✎按钮，支持多行（Shift+Enter 换行） |
| F7 | 删除待办项 | ✕按钮 |
| F8 | 新建当天日期清单 | 文件名 `YYYY-MM-DD.md` |
| F9 | 新建自定义命名清单 | 弹窗输入名称，自动补 `.md`，非法字符替换为 `_` |
| F10 | **迁移未完成项到新清单** | **支持迁移到今日清单 / 自定义清单**，目标已存在则追加 |
| F11 | 清单项置顶 | ☆/★ 切换，状态存 localStorage，**不侵入 md** |
| F12 | 清单文件置顶 | 📌 切换，状态存 localStorage，**不侵入 md** |
| F13 | 多行换行显示 | `white-space: pre-wrap` |
| F14 | 完成态不用删除线 | 仅降色 + 透明度，**不沉底**（保持原位） |
| F15 | **上下移动按钮** | ▲▼ 调整顺序（已替代拖拽，更稳定） |
| F16 | 深浅色主题切换 | 跟随系统 + 手动切换，持久化 |
| F17 | 原始 Markdown 预览 | 折叠面板，查看实际文件内容 |
| F18 | 待办/已完成数量统计 | 标题下方徽标显示 |
| F19 | **右键打开所在文件夹** | 右键文件列表项 → 资源管理器打开并选中 |

### 2.2 GitFast 工具

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| G1 | 工具切换 | Toolbar 切到 GitFast tab |
| G2 | 配置文件加载 | 系统对话框选 YAML + js-yaml 解析 + 深合并 |
| G3 | 默认配置兜底 | 内置 10 个模板 + 示例分组，开箱即用 |
| G4 | 仓库列表 | 配置驱动 + 分组折叠，折叠状态持久化 |
| G5 | 单仓库终端 | xterm.js + portable-pty，spawn Git Bash，双向通信 |
| G6 | 预设模板（无参数） | sync / fetch / status / log / branch-list / stash / commit-checklist |
| G7 | 预设模板（带参数） | branch-switch / pull-branch / rebase-branch，下拉选分支 |
| G8 | 单条命令输入 | 底部输入框，回车执行 |
| G9 | 清屏 | 顶部"清屏"按钮 |

> GitFast 的 Timer 功能已移除，定时执行需求统一由 Script Runner 承担。

### 2.3 Script Runner 工具

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| S1 | 工具切换 | Toolbar 切到 Script tab |
| S2 | 配置文件加载 | 系统对话框选 YAML |
| S3 | 脚本列表 | 左侧侧边栏展示配置中的脚本 |
| S4 | 脚本详情 | 右侧展示命令/目标/超时 |
| S5 | 目录扫描 | 扫描指定目录下一层所有含 `.git` 的子目录 |
| S6 | 指定仓库列表 | 直接配置仓库路径列表 |
| S7 | 批量执行 | 串行执行，实时进度条 |
| S8 | **脚本文件支持** | `command` 以 `.sh`/`.bash` 结尾用 bash 执行，`.bat`/`.cmd` 用 cmd 执行 |
| S9 | 执行历史 | localStorage 持久化最近 50 条，可展开看各仓库 stdout/stderr |
| S10 | 排除仓库 | directory 模式下可排除指定仓库名 |

### 2.4 Code Review 工具

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| R1 | 工具切换 | Toolbar 切到 Review tab |
| R2 | 配置文件加载 | 系统对话框选 YAML |
| R3 | 多项目卡片列表 | 手风琴模式展开/收起 |
| R4 | 分支搜索下拉选择器 | 输入框实时过滤分支，键盘上下/回车 |
| R5 | 源/目标分支选择 | 均支持搜索，目标分支默认配置值 |
| R6 | 评审标题可编辑 | 默认自动生成 `Merge src into dest` |
| R7 | 评审创建执行 | 通过环境变量注入参数调用脚本 |
| R8 | 执行结果显示 | 成功/失败 + stdout/stderr + 耗时 |
| R9 | 历史记录列表 | 文件日志，按时间倒序，最多 50 条 |
| R10 | 历史日志查看/删除 | 弹层显示完整日志内容 |
| R11 | 配置路径持久化 | localStorage 记住上次配置 |
| R12 | 启动自动加载 | 恢复上次配置文件 |

---

## 三、技术选型

### 3.1 为什么选 Tauri 2.0

| 维度 | Tauri 2.0 | Electron |
| --- | --- | --- |
| 安装包 | 2-10MB | 80-200MB |
| 空闲内存 | 20-100MB | 200-500MB |
| 后端语言 | Rust（内存安全） | Node.js |
| 渲染内核 | 系统原生 WebView | 自带 Chromium |
| 文件 I/O | 官方 plugin-fs + 自定义命令 | Node fs |

### 3.2 为什么前端选 Vue 3

- Composition API + `<script setup>` 表达力强
- Pinia 状态管理简洁直观
- Vite 开发体验丝滑，HMR 秒级热更新

### 3.3 关键决策：文件操作走自定义 Rust 命令

**背景**：Tauri 2.0 的 `plugin-fs` 默认禁止访问任何路径，需要 scope 白名单，但用户运行时才选择目录，无法预先写死 scope。

**方案**：在 [src-tauri/src/lib.rs](file:///d:/Jesse/allinone/allinone-app/src-tauri/src/lib.rs) 中实现自定义命令，直接用 `std::fs` 操作文件，绕开插件 scope 限制：

- `list_md_files` / `read_text_file` / `write_text_file` / `path_exists` / `join_path` — 清单工具用
- `open_in_explorer` — 清单右键打开文件夹
- `gitfast_pty_spawn` / `gitfast_pty_write` / `gitfast_pty_resize` / `gitfast_pty_kill` / `gitfast_branch_list` — GitFast 用
- `scripts_scan_repos` / `scripts_run_in_repo` — Script Runner 用

### 3.4 跨平台支持

| 平台 | Shell | 终端 | 打包格式 |
| --- | --- | --- | --- |
| Windows | Git Bash（GitFast）/ cmd.exe（Script Runner 默认） | WebView2 | NSIS 安装包 |
| macOS | 系统 bash | WKWebView | .app / .dmg |
| Linux | 系统 bash | WebKitGTK | AppImage / deb |

---

## 四、项目结构

```
allinone/
├── docs/
│   ├── 项目说明.md                   # 本文档（四工具总览）
│   ├── GitFast-需求文档.md           # GitFast 详细需求
│   ├── ScriptRunner-需求文档.md      # Script Runner 详细需求
│   └── Review-需求文档.md            # Code Review 详细需求
├── scripts/                          # 可执行脚本（被 Script Runner / Review 调用）
│   ├── git-review.sh                 # 评审创建脚本（读环境变量）
│   └── review-config.example.yaml    # Review 配置示例
├── .gitignore
└── allinone-app/                     # Tauri + Vue 应用
    ├── rust-toolchain.toml           # 固定 Rust 1.94.0
    ├── package.json
    ├── vite.config.ts
    ├── index.html
    ├── src/                          # 前端 (Vue 3 + TS + Pinia)
    │   ├── components/
    │   │   └── Toolbar.vue           # 顶部工具栏（四工具切换器 + 主题）
    │   ├── stores/
    │   │   ├── tool.ts               # 当前激活工具状态（持久化）
    │   │   └── theme.ts              # 主题状态
    │   ├── features/                 # 四工具代码完全分离
    │   │   ├── checklist/            # 清单工具
    │   │   │   ├── components/       # ChecklistTool / Sidebar / ChecklistView / ChecklistItem
    │   │   │   ├── lib/              # fs / markdown / pin / filePin
    │   │   │   └── stores/           # checklist
    │   │   ├── gitfast/              # GitFast 工具
    │   │   │   ├── components/       # GitFastTool / RepoList / TerminalPanel / TemplateBar / CommandInput
    │   │   │   ├── lib/              # defaultConfig / config / configLoader / template / backend
    │   │   │   ├── stores/           # gitfast
    │   │   │   └── types.ts
    │   │   ├── scripts/              # Script Runner 工具
    │   │   │   ├── components/       # ScriptTool
    │   │   │   ├── lib/              # defaultConfig / config / configLoader / backend
    │   │   │   ├── stores/           # scripts
    │   │   │   └── types.ts
    │   │   └── review/               # Code Review 工具
    │   │       ├── components/       # ReviewTool / ProjectCard / BranchSelector
    │   │       ├── lib/              # defaultConfig / config / configLoader / backend
    │   │       ├── stores/           # review
    │   │       └── types.ts
    │   ├── App.vue                   # 应用主壳（按 tool.current 切换）
    │   ├── main.ts                   # 入口（注册 Pinia）
    │   └── styles.css                # 主题变量 + 全局样式
    └── src-tauri/                    # Rust 后端
        ├── src/
        │   ├── lib.rs                # 入口，注册所有命令（含 scripts_run env 扩展、review_branch_list）
        │   └── gitfast/              # GitFast 后端（PTY 会话池 + git 命令）
        │       ├── mod.rs
        │       ├── pty.rs
        │       └── commands.rs
        ├── Cargo.toml
        ├── tauri.conf.json           # 窗口/打包配置（含 dragDropEnabled:false）
        └── capabilities/default.json # 权限声明
```

**关键设计原则**：

1. **features/ 目录隔离**：每个工具的组件、lib、store、types 都在自己的子目录，互不依赖
2. **共享最小化**：四工具仅共享 `Toolbar.vue` 和 `stores/tool.ts`，其余无交叉
3. **Rust 后端按模块划分**：`gitfast/` 是独立 mod，清单/Script/Review 命令集中在 `lib.rs`

---

## 五、核心设计

### 5.1 工具切换机制

[stores/tool.ts](file:///d:/Jesse/allinone/allinone-app/src/stores/tool.ts) 管理 `current: ToolId`（`'checklist' | 'gitfast' | 'scripts' | 'review'`），持久化到 localStorage。

[App.vue](file:///d:/Jesse/allinone/allinone-app/src/App.vue) 用 `v-show`（非 `v-if`）切换四工具，**保留各工具状态**（清单保留当前文件、GitFast 保留终端会话、Script Runner 保留历史、Review 保留配置和表单）。

### 5.2 清单：Markdown 格式（Obsidian 兼容）

文件名：`YYYY-MM-DD.md` 或自定义名称（如 `项目计划.md`）

内容模板：

```markdown
# 2026-06-27 清单

## 待办

- [ ] 完成技术选型文档
- [ ] 搭建项目骨架
- [x] 调研框架对比

## 备注

- 任何自由文本均可，Obsidian 会正常渲染
```

解析规则（[features/checklist/lib/markdown.ts](file:///d:/Jesse/allinone/allinone-app/src/features/checklist/lib/markdown.ts)）：

- 行首匹配 `^(\s*)[-*+]\s+\[( |x|X)\]\s+(.*)$` 即为清单项
- 非清单行原样保留，编辑时不破坏标题/备注结构
- `buildChecklistContent(title, items)`：title 既支持日期也支持自定义名称

### 5.3 清单：双层置顶（不侵入 md）

| 层级 | 存储模块 | 标识方式 | 视觉 |
| --- | --- | --- | --- |
| 清单项置顶 | [pin.ts](file:///d:/Jesse/allinone/allinone-app/src/features/checklist/lib/pin.ts) | `文件路径 + :: + 文本内容` | ★ |
| 清单文件置顶 | [filePin.ts](file:///d:/Jesse/allinone/allinone-app/src/features/checklist/lib/filePin.ts) | `文件绝对路径` | 📌 |

**为什么用文本/路径而非行号**：避免文件编辑后行号变化导致置顶错位。

**清单项展示规则**（`displayItems`）：
1. 置顶项 → 顶部
2. 非置顶项 → 按文件原顺序
3. 已完成项 **不再沉底**，保持原位

### 5.4 清单：上下移动按钮（替代拖拽）

**历史**：原 v0.4 用 HTML5 拖拽排序，但 Tauri 2.0 的 `dragDropEnabled: false` + 混合置顶场景下行号桥接不稳定。

**现方案**（[ChecklistItem.vue](file:///d:/Jesse/allinone/allinone-app/src/features/checklist/components/ChecklistItem.vue)）：
- 每个清单项右侧操作区加 ▲▼ 两个按钮
- 第一项 ▲ 禁用，最后一项 ▼ 禁用
- 基于 `displayItems` 视觉顺序交换相邻项
- 用 `reorderItems(parsed, newOrder)` 按新顺序回写清单行（非清单行不动）

### 5.5 清单：迁移未完成项

[stores/checklist.ts](file:///d:/Jesse/allinone/allinone-app/src/features/checklist/stores/checklist.ts) 提供：

- `carryoverToToday()` — 迁移到今日清单（`YYYY-MM-DD.md`），已存在则追加到"待办"段末尾
- `carryoverToNamed(name)` — 迁移到自定义命名清单，已存在则追加

UI 入口：ChecklistView 顶部"迁移未完成 ▾"下拉菜单。

### 5.6 清单：右键打开所在文件夹

[Sidebar.vue](file:///d:/Jesse/allinone/allinone-app/src/features/checklist/components/Sidebar.vue) 文件项 `@contextmenu` 弹浮层菜单，仅一项"打开所在文件夹"。

后端 [lib.rs](file:///d:/Jesse/allinone/allinone-app/src-tauri/src/lib.rs) 的 `open_in_explorer` 命令跨平台：
- Windows：`explorer.exe /select,<path>`
- macOS：`open -R <path>`
- Linux：`xdg-open <父目录>`

### 5.7 GitFast：PTY 终端架构

```
xterm.js（前端）  ←emit/output→  Rust（portable-pty）  ←→  bash 进程
     ↑                                  ↓
     └──invoke/write────────────────────┘
```

- **前端**：xterm.js + FitAddon，自适应尺寸
- **后端**：[gitfast/pty.rs](file:///d:/Jesse/allinone/allinone-app/src-tauri/src/gitfast/pty.rs) 用 `portable-pty` 创建伪终端，每仓库一个 session
- **Shell**：Windows 用 Git Bash（`detect_bash()` 找路径），Mac/Linux 用系统 bash
- **通信**：Rust 读 PTY → Tauri emit `gitfast_pty_output` → xterm 写入；xterm 输入 → invoke `gitfast_pty_write` → Rust 写 PTY

### 5.8 GitFast：配置文件三层覆盖

```
1. 代码内置默认配置（DEFAULT_CONFIG，不可变）
       ↓ 被覆盖
2. 用户加载的 YAML 配置文件（可选）
       ↓ 被覆盖
3. localStorage 运行时状态（UI 临时修改）
```

合并策略：深合并。**配置文件不回写**，用户改配置直接编辑 YAML 文件。

### 5.9 Script Runner：执行模型

与 GitFast 的关键区别：

| 维度 | GitFast | Script Runner |
| --- | --- | --- |
| 执行方式 | PTY（交互式终端） | `std::process::Command`（一次性执行） |
| 仓库数 | 单仓库 | 多仓库批处理 |
| 反馈方式 | 实时终端输出 | 历史记录 + stdout/stderr |
| 并发 | 单仓库 | 串行（P0） |
| 脚本文件 | 不支持 | 支持 `.sh` / `.bat` |

### 5.11 Script Runner：脚本文件识别

[lib.rs](file:///d:/Jesse/allinone/allinone-app/src-tauri/src/lib.rs) 的 `build_shell_command` 按脚本后缀自动选择执行器：

| 脚本结尾 | Windows 执行器 | Mac/Linux 执行器 |
| --- | --- | --- |
| `.sh` / `.bash` | Git Bash（`bash.exe <file>`） | 系统 `bash <file>` |
| `.bat` / `.cmd` | `cmd.exe /C <file>` | 不支持（报错） |
| 其他（命令字符串） | `cmd.exe /C <command>` | `sh -c <command>` |

Windows 上的 bash 路径通过 `detect_bash()` 检测（复用 GitFast 逻辑）。

### 5.12 Script Runner：定时调度与文件日志

- **调度规则**：`interval`（每 N 分钟）/ `daily`（每天 HH:MM）
- **调度器**：浏览器原生 `setInterval`，零新依赖
- **运行时启停**：内存覆盖（`runtimeEnabled` Map），不落盘
- **文件日志**：`<logDir>/<taskId>_<YYYYMMDD_HHMMSS>.log`，含 stdout/stderr/exit_code/duration
- **日志查看**：UI 列出最近 50 条，可查看完整内容、删除

### 5.13 Code Review：参数化脚本执行

Code Review 复用 Script Runner 的 `scripts_run` 命令（已扩展支持 `env` 参数），通过环境变量注入项目信息：

```
用户选源/目标分支
  ↓
store 组装 env（REPO_ID/FULL_NAME/SRC_BRANCH/DEST_BRANCH/PLATFORM_URL/ACCESS_TOKEN/REVIEW_SUBJECT）
  ↓
invoke scripts_run({ script, workdir, env, ... })
  ↓
Rust Command::new(bash).envs(env).spawn()
  ↓
脚本读环境变量 → 调 API → 输出评审 URL
  ↓
结果回传 → UI 显示 + 写文件日志
```

**与 Script Runner 的区别**：

| 维度 | Script Runner | Code Review |
| --- | --- | --- |
| 触发 | 定时 + 手动 | 仅手动 |
| 参数 | 无（配置写死） | 运行时选分支 |
| 配置粒度 | 任务级 | 项目级 + 全局 platform |
| UI | 任务卡片 | 项目卡片 + 分支选择器 |

### 5.14 Code Review：分支搜索选择器

[BranchSelector.vue](file:///d:/Jesse/allinone/allinone-app/src/features/review/components/BranchSelector.vue) 实现可搜索的下拉选择：

- 未展开：显示当前选中值
- 展开后：输入框实时过滤分支列表
- 键盘导航：↑↓ 选择，回车确认，Esc 取消
- 自由输入：无匹配项时按回车可直接用输入值
- 分支列表来源：`review_branch_list(repoPath)` 命令（git branch -a 去重）

---

## 六、运行与打包

### 6.1 环境要求

- Node.js 18+（实测 24.15.0）
- Rust 1.94.0（项目固定版本，见 `rust-toolchain.toml`）
- MSVC Build Tools 2022（Windows 编译 Rust 必需）
- Git for Windows（GitFast 和 Script Runner 的 .sh 脚本依赖）

> ⚠️ **避开 Rust 1.95.0**：该版本在 Windows 上 build script 调用 `Command::output()` 存在 panic，已通过 `rust-toolchain.toml` 固定 1.94.0 规避。

### 6.2 开发模式

```powershell
cd D:\Jesse\allinone\allinone-app
npm install        # 首次
npm run tauri dev
```

首次启动编译 Rust 依赖约 5-10 分钟，后续增量编译很快。前端 HMR 秒级热更新。

### 6.3 打包

```powershell
cd D:\Jesse\allinone\allinone-app
npm run tauri build
```

产物位置：

```
src-tauri\target\release\
├── allinone-app.exe                              # 绿色版，双击即用
└── bundle\nsis\allinone-app_0.1.0_x64-setup.exe  # NSIS 安装包（Windows）
```

打包配置（[tauri.conf.json](file:///d:/Jesse/allinone/allinone-app/src-tauri/tauri.conf.json)）：

- `targets: "all"` — 自动按当前系统生成对应格式
- Windows：NSIS 安装包（`installMode: "perMachine"`，中英双语）
- macOS：`.app` / `.dmg`
- `dragDropEnabled: false` — 禁用原生拖放转发

### 6.4 仅编译 release exe（跳过安装包打包）

如果网络问题导致 `tauri build` 下载 NSIS 失败，可只编译 exe：

```powershell
cd D:\Jesse\allinone\allinone-app\src-tauri
cargo build --release
```

产物：`target\release\allinone-app.exe`，功能完整，体积比 debug 小。

### 6.5 ⚠️ 沙箱限制说明

Trae IDE 沙箱内无法编译 Rust（build script 子进程管道受限），**所有 `npm run tauri dev` / `npm run tauri build` 必须在沙箱外的常规 PowerShell 执行**。沙箱内仅可运行 `npm run build`（前端构建）和 `npx vue-tsc --noEmit`（类型检查）。

---

## 七、使用指南

### 7.1 清单工具

**首次使用**：
1. 启动应用 → 顶部默认在"📋 清单"tab
2. 点"选择文件夹"，选一个存放清单的目录
3. 点"＋ 今日"生成 `2026-06-27.md`，或点"＋ 自定义"输入名称

**日常操作**：

| 操作 | 方式 |
| --- | --- |
| 勾选完成 | 点击项前方框 |
| 编辑文本 | 双击文本，或点✎ |
| 多行输入 | 输入时 Shift+Enter 换行 |
| 置顶清单项 | 点项右侧☆变★ |
| 置顶清单文件 | 点侧边栏文件项右侧☆变★ |
| 调整顺序 | 点项右侧 ▲▼ |
| 删除 | 点✕ |
| 迁移未完成 | 顶部"迁移未完成 ▾" → 选"今日"或"自定义" |
| 打开所在文件夹 | 右键文件列表项 |
| 切换主题 | 右上角🌙/☀️ |

### 7.2 GitFast 工具

**首次使用**：
1. 顶部切到"⚡ GitFast"tab
2. 点"加载配置" → 选 YAML 配置文件（格式见 [GitFast-需求文档.md](file:///d:/Jesse/allinone/docs/GitFast-需求文档.md)）
3. 左侧出现仓库列表，点仓库 → 终端启动 bash
4. 点模板按钮（sync/fetch/status 等）或底部输入框执行 git 命令

> GitFast 的 Timer 功能已移除。如需定时执行，请使用"📜 Script Runner"工具。

### 7.3 Script Runner 工具

**首次使用**：
1. 顶部切到"📜 Script"tab
2. 点"加载配置" → 选 YAML 配置文件（格式见 [ScriptRunner-需求文档.md](file:///d:/Jesse/allinone/docs/ScriptRunner-需求文档.md)）
3. 任务卡片显示脚本名称、规则、超时
4. 点"立即执行" → 执行完成后显示结果
5. 点"查看日志" → 弹层显示日志列表，可查看/删除历史日志

### 7.4 Code Review 工具

**首次使用**：
1. 顶部切到"🔀 Review"tab
2. 点"选择配置文件" → 选 YAML 配置文件（格式见 [Review-需求文档.md](file:///d:/Jesse/allinone/docs/Review-需求文档.md)），可参考 `scripts/review-config.example.yaml`
3. 配置加载后显示项目卡片列表
4. 点项目卡片展开 → 自动加载分支列表
5. 选源分支、目标分支（均支持搜索，目标分支默认配置值）
6. 可选编辑评审标题（默认 `Merge <src> into <dest>`）
7. 点"创建评审" → 调用脚本，结果面板显示成功/失败 + 评审 URL
8. 点"查看历史" → 列出该项目的评审创建日志，可查看/删除

**配置文件示例**（`scripts/review-config.example.yaml`）：

```yaml
version: 1
logDir: ./logs
platform:
  url: https://devops.tone.tcl.com
  accessToken: <your-token>
script: D:/Jesse/allinone/scripts/git-review.sh
projects:
  - id: plm-pdm-service
    name: PLM-PDM 服务
    repoPath: D:/code/plm-pdm-service
    repoId: "950381585941725184"
    fullName: rd-plm/plm-pdm-service
    defaultDestBranch: tct-develop-xjx2
```

### 7.5 与 Obsidian 协同

- 用 Obsidian 打开同一文件夹，可作为日报 Vault
- 应用内的修改 Obsidian 实时可见（因直接写 .md 文件）
- Obsidian 中的修改，应用切换清单时重新读取即同步
- **置顶状态仅在应用内可见**，Obsidian 看到的是纯净 md

---

## 八、常见问题

**Q：点击新建清单无反应？**
A：已修复。原因是 Tauri 2.0 fs 插件 scope 限制。现改为自定义 Rust 命令绕开 scope。

**Q：Rust 编译报 "link.exe not found"？**
A：缺 MSVC 工具链。装 Visual Studio Build Tools 2022，勾选"使用 C++ 的桌面开发"。

**Q：Rust 编译报 "code: 0 操作成功完成" panic？**
A：Rust 1.95.0 工具链 bug。项目已用 `rust-toolchain.toml` 固定 1.94.0。

**Q：tauri build 下载 NSIS 失败（TLS 错误）？**
A：网络问题。方案1：设置代理 `$env:HTTPS_PROXY = "http://127.0.0.1:7890"`。方案2：用 `cargo build --release` 只编译 exe。

**Q：GitFast 终端启动失败？**
A：检查是否安装 Git for Windows。`detect_bash()` 会从 `C:\Program Files\Git\bin\bash.exe` 查找，找不到退回 PATH 中的 bash。

**Q：Script Runner 跑 .sh 脚本失败？**
A：同上，依赖 Git Bash。脚本内的相对路径相对的是**仓库目录**（`current_dir` 已设为仓库路径）。

**Q：置顶状态会同步到 Obsidian 吗？**
A：不会。项级和文件级置顶均仅存应用 localStorage，md 文件保持纯净。

**Q：自定义清单名称输入了非法字符？**
A：自动替换 `\/:*?"<>|` 为 `_`。如输入 `a/b:c` 会生成 `a_b_c.md`。

**Q：GitFast 的定时任务（Timer）去哪了？**
A：已移除。定时执行需求统一由"📜 Script Runner"工具承担，功能更强大（支持任意脚本、完整日志文件、可查看历史）。
